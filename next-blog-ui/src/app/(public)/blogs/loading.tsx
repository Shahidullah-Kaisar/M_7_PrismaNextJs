import Loading from '@/components/ui/Loading';
import React from 'react';

const loading = () => {
    return (
        <div className='py-32'>
            <Loading/>
        </div>
    );
};

export default loading;